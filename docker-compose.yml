services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: analyze-github-activity
    working_dir: /workspace
    volumes:
      - .:/workspace
    env_file:
      - .env
    environment:
      RENV_PATHS_CACHE: /workspace/renv/cache
    command: ["R", "-q"]
