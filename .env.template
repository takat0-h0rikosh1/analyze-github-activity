# GitHub Personal Access Token (with repo/read:org scopes)
GITHUB_TOKEN=

# Target GitHub Organization (e.g., your-company)
GITHUB_ORG=

# Optional: Override GitHub API base URL (for GitHub Enterprise)
# GITHUB_API_URL=https://github.example.com/api/v3

# Optional: Comma-separated list of agent GitHub usernames
# GITHUB_AGENT_USERS=github-copilot, coderabbit-ai, claude-code

# Optional: Limit processing to specific repositories (comma-separated owner/name)
# GITHUB_TARGET_REPOS=owner/repo-one,owner/repo-two

# Optional: Override approval thresholds per repo (repo=approvals, default 1)
# GITHUB_APPROVAL_THRESHOLDS=owner/repo=2,owner/another-repo=3

# Optional: Load approval thresholds from external file (one `repo=number` per line)
# GITHUB_APPROVAL_THRESHOLDS_FILE=./config/approval_thresholds.conf

# Optional: GitHub API throttling controls
# GITHUB_REQUEST_PAUSE_SEC=0.25
# GITHUB_RATE_THRESHOLD=50
# GITHUB_RATE_RESET_PADDING_SEC=5

# Optional: Output directory inside container (defaults to data/)
# OUTPUT_DIR=data

# Optional: Directory for visualization outputs (defaults to output/viz)
# OUTPUT_VIZ_DIR=output/viz

# Optional: Plot label language (`en` or `ja`, default: en)
# PLOT_LABEL_LANGUAGE=en

# Optional: Override the AI introduction date (ISO format). Defaults to 2025-06-18
# AI_T0=2025-06-18
