# ============================================================================
# 必須設定
# ============================================================================

# GitHub Personal Access Token (repo, read:org スコープが必要)
GITHUB_TOKEN=

# 分析対象の GitHub Organization 名
GITHUB_ORG=

# ============================================================================
# GitHub API 設定
# ============================================================================

# GitHub API のベース URL (GitHub Enterprise Server を使用する場合に指定)
# GITHUB_API_URL=https://github.example.com/api/v3

# AI エージェント（Bot）として扱う GitHub ユーザー名（カンマ区切り）
# 例: GitHub Copilot, CodeRabbit, Claude Code など
# GITHUB_AGENT_USERS=github-copilot,coderabbit-ai,claude-code

# 分析対象リポジトリを限定する場合に指定（カンマ区切り、owner/repo 形式）
# 未指定の場合は Organization 内の全リポジトリが対象
# GITHUB_TARGET_REPOS=owner/repo-one,owner/repo-two

# リポジトリごとの承認数しきい値（カンマ区切り、repo=threshold 形式）
# デフォルトは全リポジトリで 1
# GITHUB_APPROVAL_THRESHOLDS=owner/repo=2,owner/another-repo=3

# 承認数しきい値を外部ファイルから読み込む場合のパス
# ファイル形式: 1行ごとに repo=number を記載
# GITHUB_APPROVAL_THRESHOLDS_FILE=./config/approval_thresholds.conf

# User-Agent ヘッダー（デフォルト: analyze-github-activity/0.1）
# GITHUB_USER_AGENT=my-company-analytics/1.0

# 処理するリポジトリ数の上限（テスト実行時に有用）
# GITHUB_REPO_LIMIT=5

# ============================================================================
# AI/データ分析設定
# ============================================================================

# AI ツール導入日（T0: Time Zero）ISO 8601 形式 (YYYY-MM-DD)
# この日付を境に pre_ai / post_ai 期間を分類
# デフォルト: 2025-06-18
# AI_T0=2025-06-18

# T0 から遡ってデータを取得する日数
# デフォルト: 180（6ヶ月）
# 推奨値: 試験実行=30-60, 通常分析=180, 長期分析=365
# AI_LOOKBACK_DAYS=180

# ============================================================================
# ページネーション制御
# ============================================================================
# GitHub API は 1 リクエストあたり最大 100 件を返します。
# 各環境変数で最大ページ数を制御できます（ページ数 × 100 = 最大取得件数）

# リポジトリ一覧取得の最大ページ数（デフォルト: 1 = 最大100リポジトリ）
# GITHUB_MAX_REPO_PAGES=1

# 各リポジトリの PR 一覧取得の最大ページ数（デフォルト: 100 = 最大10,000PR）
# 重要: この値が小さすぎると古い PR が取得できず、データが欠落します
# GITHUB_MAX_PR_PAGES=100

# 各 PR のレビュー一覧取得の最大ページ数（デフォルト: 10 = 最大1,000レビュー）
# GITHUB_MAX_REVIEW_PAGES=10

# 各 PR のコメント一覧取得の最大ページ数（デフォルト: 10 = 最大1,000コメント）
# GITHUB_MAX_COMMENT_PAGES=10

# 各リポジトリのコミット一覧取得の最大ページ数（デフォルト: 50 = 最大5,000コミット）
# GITHUB_MAX_COMMIT_PAGES=50

# ============================================================================
# レート制限・スロットリング制御
# ============================================================================
# GitHub API にはレート制限があります（通常 5,000 リクエスト/時間）

# 各 API リクエスト後の待機時間（秒）
# デフォルト: 0.25（250ミリ秒）
# GITHUB_REQUEST_PAUSE_SEC=0.25

# レート制限の残り回数がこの値を下回った場合、リセット時刻まで待機
# デフォルト: 50
# GITHUB_RATE_THRESHOLD=50

# レート制限リセット時刻に追加する余裕時間（秒）
# デフォルト: 5
# GITHUB_RATE_RESET_PADDING_SEC=5

# ============================================================================
# 出力設定
# ============================================================================

# CSV ファイルなどのデータファイル出力ディレクトリ
# デフォルト: output/files
# OUTPUT_DIR=output/files

# 可視化ファイル（PNG グラフ）の出力ディレクトリ
# デフォルト: output/viz
# OUTPUT_VIZ_DIR=output/viz

# ============================================================================
# 可視化設定
# ============================================================================

# グラフのラベル言語（`en` または `ja`）
# デフォルト: en
# PLOT_LABEL_LANGUAGE=en
